#include <bits/stdc++.h>
using namespace std;
#define ll long long

// Just read the whole problem and initally quite dumbfounded, not sure what data structure to use

void solve() {
    int p, m;
    cin >> p >> m;
    int moves[p];
    for (int i = 0; i < p; i++)
        cin >> moves[i];
    

}

int main(void) {
    ios::sync_with_stdio(0);
    cin.tie(0);

    solve();
}